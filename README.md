Apache Spark Group Flatten Example
==================================

Example of using Apache Spark to flatten out a hierarchy of objects.
Consumes the `example.json` file and outputs a json directory
in `build/output.json`

(Tested using JDK 11)

Running
-------

```
./gradlew clean run
```